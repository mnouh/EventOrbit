<?php

	$hostname = 'localhost:/tmp/mysql.sock';
	$db_name = 'notes';
	$db_username = 'root';
	$db_password = 'chief4625';
	$status = null;
	
		$status = mysql_connect ($hostname, $db_username, "chief4625");
		mysql_selectdb ($db_name);
		
		if (!$status) {
			die ( 'Could not connect: ' . mysql_error () );
		}
		//echo 'Connected successfully';


	?>
